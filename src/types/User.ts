export interface UserForm {
    pseudo: string;
    email: string;
    password: string;
}

export interface User {
    id: number;
    user: {};
    pseudo: string;
    user_tag: string;
    email: string,
    user_role: string
}